<!-- displays the post#index html while render the post partial -->
<div class="row">
	<div class="col-md-8">
		<h1>Popular</h1>
		<p class="lead">Active posts this week</p>
		<%= render @post %>
		<%= will_paginate @post %>
	</div>
	<div class="col-md-4">
	</div>
</div>

